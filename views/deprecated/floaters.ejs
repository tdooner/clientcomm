<% include ../partials/headers/header %>

Unassociated Contact Information:
<ul>
  <% for (var i = 0; i < comms.length; i++) { %>

    <li>
      <% if (comms[i].type == "landline" || comms[i].type == "cell") { %>
        <%= "(" + comms[i].value.substr(1, 3) + ") " + comms[i].value.substr(4, 3) + "-" + comms[i].value.substr(7, 4) %>
      <% } %>
      [Created: <%= comms[i].created %>]
    </li>

  <% } %>
</ul>

<% include ../partials/footers/footer %>
